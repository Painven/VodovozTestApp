<Window x:Class="VodovozTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:VodovozTestApp.ViewModels"
        xmlns:v="clr-namespace:VodovozTestApp.Views"
        xmlns:beh="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:i="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel, IsDesignTimeCreatable=True}"
        Title="{Binding Title}" Height="700" Width="1200">
    <beh:Interaction.Triggers>
        <beh:EventTrigger EventName="Loaded">
            <beh:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </beh:EventTrigger>
    </beh:Interaction.Triggers>
    <Grid>
        <!--<Grid.Background>
            <ImageBrush ImageSource="/Assets/water-drop.png" TileMode="Tile" Viewport="0,0,64,64" ViewportUnits="Absolute"/>
        </Grid.Background>-->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Главное меню-->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{StaticResource MainPanelBackground}">
                <Button Command="{Binding ToolExampleCommand}" Style="{StaticResource ToolButtonStyle}">
                    <i:PackIconFontAwesome Kind="WaterSolid" Style="{StaticResource SmallIconSize}"/>
                </Button>
                <Button Command="{Binding ToolExampleCommand}" Style="{StaticResource ToolButtonStyle}">
                    <i:PackIconFontAwesome Kind="AirFreshenerSolid" Style="{StaticResource SmallIconSize}"/>
                </Button>
                <Button Command="{Binding ToolExampleCommand}" Style="{StaticResource ToolButtonStyle}">
                    <i:PackIconFontAwesome Kind="HandHoldingWaterSolid" Style="{StaticResource SmallIconSize}"/>
                </Button>
                <Button Command="{Binding ToolExampleCommand}" Style="{StaticResource ToolButtonStyle}">
                    <i:PackIconFontAwesome Kind="CogsSolid" Style="{StaticResource SmallIconSize}"/>
                </Button>
            </StackPanel>

            <!-- Основные секции -->
            <Grid Grid.Row="1" Background="{StaticResource MainPanelBackground}" Margin="0 4 0 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Раздел подразделений -->
                <v:DepartmentsListView Grid.Row="0" DataContext="{Binding Departments}" />

                <!-- Раздел сотрудников -->
                <Grid Grid.Column="1" Margin="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <i:PackIconFontAwesome Kind="UserSolid" Style="{StaticResource SectionIconStyle}" />
                        <TextBlock Text="Сотрудники" 
                                   Style="{StaticResource SectionTextBoxStyle}" 
                                   VerticalAlignment="Center"/>
                        <Button Command="{Binding AddNewEmployeeCommand}" 
                                Style="{StaticResource SectionButtonStyle}">
                            <i:PackIconFontAwesome Kind="PlusSolid" Style="{StaticResource ExtraSmallIconSize}" />
                        </Button>
                    </StackPanel>

                    <ListBox Grid.Row="1" Style="{StaticResource SectionListBoxStyle}">

                    </ListBox>

                </Grid>

                <!-- Раздел заказов -->
                <Grid Grid.Column="2" Margin="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="2" Orientation="Horizontal" HorizontalAlignment="Center">
                        <i:PackIconFontAwesome Kind="BoxSolid" Style="{StaticResource SectionIconStyle}" />
                        <TextBlock Text="Заказы" 
                                   Style="{StaticResource SectionTextBoxStyle}" 
                                   VerticalAlignment="Center"
                                   />
                        <Button Command="{Binding AddNewOrderCommand}" 
                                Style="{StaticResource SectionButtonStyle}">
                            <i:PackIconFontAwesome Kind="PlusSolid" Style="{StaticResource ExtraSmallIconSize}" />
                        </Button>
                    </StackPanel>

                    <ListBox Grid.Row="1" Style="{StaticResource SectionListBoxStyle}">

                    </ListBox>

                </Grid>
            </Grid>
        </Grid>
    </Grid>
    
</Window>
